.project
.DS_Store
.buildpath
.versions
node_modules
npm-debug.log
build
build/*
build/**
v3/build
v3/build/*
v3/build/**
v3/node_modules
v3/documentation/slimerjs-*/**
v3/documentation/api/**
v3/documentation/download/*.zip
v3/benchmark/suite/cytoscape.js
benchmark/results/
*.tsbuildinfo

# Playwright (both projects)
test-results/
playwright-report/
blob-report/
playwright/.cache/
# agent worktrees (Claude Code's `.claude/worktrees/`, Codex's equivalent).
# Claude Code writes the same rule into `.git/info/exclude`, which is local to
# one clone and never travels; this is the portable half.
/.claude/worktrees/

# the status site build output (root only — `scripts/status/` is source)
/status
